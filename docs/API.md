# API Reference

- GET /health -> health status
- GET /assets -> list assets
- GET /assets/{asset_id} -> asset detail
- POST /predict/failure-risk -> classify failure risk
- POST /predict/rul -> predict remaining useful life
- POST /predict/anomaly -> anomaly flagging
- POST /predict/forecast -> forecast sensor values
